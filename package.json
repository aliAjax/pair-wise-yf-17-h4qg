{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 62005",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 62005",
    "check": "esbuild scripts/check.ts --bundle --platform=node --format=esm --outfile=/tmp/organ-check.mjs && node /tmp/organ-check.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0"
  }
}
